# TypeScript Server Memory Configuration

# Increase TS Server memory to 4GB
# Add to your VS Code settings.json or create .npmrc in project root
# NODE_OPTIONS must be set in your shell profile or VS Code terminal settings

# For VS Code, add this to settings.json instead:
# "typescript.tsserver.maxMemory": "4096"

# Alternative: Set in package.json scripts
# "scripts": {
#   "ts:check": "tsc --noEmit --max-old-space-size=4096"
# }
